# Neo Editor

A level editor for Neodash.

## Usage

`WASD` to move.

`SPACE` to go up.

`SHIFT` to go down.

`Z` to toggle the mouse.

`RIGHT CLICK` to open the context menu.

## Development

```bash
bun i
bun dev
```
